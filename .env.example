# Supabase — service-role key is used SERVER-SIDE ONLY (never expose to the browser).
# Copy these from the driver-context-manager/.env file.
# Do NOT add a NEXT_PUBLIC_ prefix to the service-role key.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
